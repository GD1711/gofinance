# Variáveis de ambiente (exemplo)
# Copiar para .env e ajustar valores

# API
API_TITLE="Financial Protocol API"
API_VERSION="1.0.0"
HOST="0.0.0.0"
PORT=8000
RELOAD=false
WORKERS=4

# CORS (separar por vírgula)
CORS_ORIGINS="http://localhost:3000,https://seuapp.com"

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Validação
MAX_TARGET_AMOUNT=1000000.0
MIN_TARGET_AMOUNT=10.0
MAX_PERIODS=120
MIN_PERIODS=3

# Logging
LOG_LEVEL="info"

# Database (futuro)
# DATABASE_URL="postgresql://user:password@localhost:5432/financial_protocol"

# Redis (futuro)
# REDIS_URL="redis://localhost:6379/0"

# Auth (futuro)
# SECRET_KEY="your-secret-key-here-change-in-production"
# ALGORITHM="HS256"
# ACCESS_TOKEN_EXPIRE_MINUTES=30
